## 56. File Name Selection by Pattern Matching: Globbing with `*`

### the tasks 
* we have folder of images 
* move the images to a new folder (images)
  * the last parameter is the distination folder
  * use expansion instead of typing each file name
* disable globbing 
* 
### file name expansion 
* this allows to access multiple files with a single command
* expansion happends before executing the command 